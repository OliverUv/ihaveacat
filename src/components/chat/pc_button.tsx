import * as React from 'react';
import { FormattedHTMLMessage } from 'react-intl';

interface PCButtonProps {
}

interface PCButtonState {
}

export class PCButton extends React.Component<PCButtonProps, PCButtonState> {
    public render() {
        return (
            <p />
        );
    }
}
