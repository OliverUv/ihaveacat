import * as React from 'react';
import { FormattedHTMLMessage } from 'react-intl';

interface NPCImageProps {
}

interface NPCImageState {
}

export class NPCImage extends React.Component<NPCImageProps, NPCImageState> {
    public render() {
        return (
            <p />
        );
    }
}
